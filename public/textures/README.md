# 달 표면 텍스처 자리

NASA SVS **CGI Moon Kit** 의 색상 맵을 2K 이하로 축소한 파일(예: `moon-color-2k.jpg`)을 넣고
`src/content/assets.ts` 의 `MOON_TEXTURE.src` 와 `credit` 을 채운다.

- 이용 조건을 확인한 공식 텍스처만 사용한다.
- 파일이 없으면 3D 모형은 균일한 회색 재질로 표시되며 크레디트 자리는 비어 있다.
